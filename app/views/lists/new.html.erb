<h2>Add a new List</h2>
<div>
  <%= form_for(@list) do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    <%= f.submit %>
  <% end %>
</div>

<div>
  <p><%= link_to 'Back to Movie Lists', lists_path %></p>
</div>
