<h2><%= @list.name %></h2>



<div class="cards">
  <% @list.movies.each do |movie| %>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= movie.poster_url %>)">

  <div class="card-body">
    <h3 class="card-title"><%= movie.title %></h5>
    <p><%= truncate(movie.overview, length: 60, separator: ' ') %></p>
    <p><%= '⭐' * movie.rating %></p>
  </div>
    </div>
  <% end %>
</div>


<p><%= link_to 'New bookmark', new_list_bookmark_path(@list) %> | <%= link_to 'Back to Movie Lists', lists_path %></p>
