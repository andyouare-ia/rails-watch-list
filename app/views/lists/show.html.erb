<h2><%= @list.name %></h2>

<% @movies = Bookmark.find(@list.id).movies %>

<% @movies.each do |movie| %>

<p><%= movie.title %></p>

<% end %>

<p><%= link_to 'New bookmark', new_list_bookmark_path(@list) %></p>
